// Placeholder — real authentication (Supabase) lands in T-003. Members are
// registered by their leader; there is no public sign-up.
export default function LoginPage() {
  return (
    <main className="mx-auto flex max-w-md flex-1 flex-col justify-center gap-3 px-6 py-16">
      <h1 className="text-2xl font-bold tracking-tight">Sign in</h1>
      <p className="text-muted">
        Sign-in is coming with the authentication task. There is no public
        sign-up. Members are registered by their leader.
      </p>
    </main>
  );
}
